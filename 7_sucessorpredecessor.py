#Program that prints the sucessor and the predecessor of the number digited by the user.

number = int(input("Digit one number: "))
predecessor = number - 1
sucessor = number + 1

print("About the number {}, the predecessor is  {} and the sucessor is {}".format(number,predecessor,sucessor))
